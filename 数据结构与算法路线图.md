# 📚 数据结构与算法 · 全广度通识路线图（零基础防遗漏版）

## Phase 0 — 绪论与复杂度基础

### 核心通识知识点
- 什么是数据结构（逻辑结构 vs 物理存储结构）
- 什么是算法（五大特性：有穷、确定、可行、输入、输出）
- **时间复杂度**（最坏/最好/平均，大O定义）
- **空间复杂度**（原地算法概念）
- 递归的入门概念（递推与回归）

### 一句话本质
算法好坏不看代码长短，看数据量增长时机器指令的增长趋势。

### 对应Java底层影子
`Arrays.sort()` 在不同数据量下会切换排序策略（阈值）

---

## Phase 1 — 线性表（一）：顺序存储

### 核心通识知识点
- 线性表的定义与特征
- **顺序表（数组）** 的内存连续性
- 顺序表的基本操作（增、删、改、查）
- 顺序表的扩容机制（静态 vs 动态）
- **多维数组**的存储优先（行优先/列优先）

### 一句话本质
数组是一段连续内存，通过"基址+偏移量"直接寻址。

### 对应Java底层影子
`ArrayList`（动态数组）

---

## Phase 2 — 线性表（二）：链式存储

### 核心通识知识点
- **单链表**（头指针、尾指针、哨兵节点）
- **双链表**（前驱/后继指针）
- **循环链表**（约瑟夫环问题）
- 顺序表 vs 链表的**全面对比**（存取效率、插入删除、内存碎片）

### 一句话本质
链表是内存不连续的节点，靠指针串联，插入删除只需改指针。

### 对应Java底层影子
`LinkedList`（双向链表）

---

## Phase 3 — 受限线性表：栈与队列

### 核心通识知识点
- **栈（Stack）**：LIFO（后进先出），入栈/出栈/取栈顶
- **队列（Queue）**：FIFO（先进先出），入队/出队/取队头
- **循环队列**（解决假溢出，取模运算）
- **双端队列（Deque）**：两端都可进出
- 栈与队列的**应用场景**（函数调用栈、消息队列、缓冲区）

### 一句话本质
栈是"后进先出"的受限表，队列是"先进先出"的受限表。

### 对应Java底层影子
`ArrayDeque`（循环数组双端队列）、`Stack`（遗留类）

---

## Phase 4 — 字符串（串）

### 核心通识知识点
- 串的定义与基本操作（子串、拼接、比较）
- **暴力匹配（BF算法）**
- **KMP算法**（通识层：知道next数组是干啥的，不要求立马手撕）
- 字符串在Java中的不可变性（String常量池）

### 一句话本质
字符串是特殊的线性表，元素限定为字符。

### 对应Java底层影子
`String`、`StringBuilder`

---

## Phase 5 — 排序算法（全览）

### 核心通识知识点
- **O(n²)级**：冒泡、选择、插入（含希尔）
- **O(n log n)级**：归并、快速、堆排序
- **O(n)级**：计数、桶、基数排序
- **稳定性**：哪些排序稳定，哪些不稳定，为什么
- **排序的工程应用**：混合排序（如TimSort）

### 一句话本质
排序是算法最重要的基础操作，没有"万能最优排序"，只有"最适合数据特征的排序"。

### 对应Java底层影子
`Arrays.sort()`（基础类型用双轴快排，对象用TimSort）

---

## Phase 6 — 树形结构（一）：基础二叉树

### 核心通识知识点
- 树的定义与术语（根/叶子/深度/度）
- **二叉树**：满二叉树、完全二叉树、二叉链表存储
- **四种遍历**：前序、中序、后序（递归）、层序（BFS）
- **线索二叉树**（利用空指针加速遍历）

### 一句话本质
二叉树是每个节点最多两个分叉的递归结构。

### 对应Java底层影子
`TreeMap` / `TreeSet` 的底层树基础

---

## Phase 7 — 树形结构（二）：二叉搜索树与平衡树

### 核心通识知识点
- **二叉搜索树（BST）**：左小右大的性质，增删查
- **AVL树**：平衡因子的概念，四种旋转（LL/RR/LR/RL）
- **红黑树（RBT）**：五大性质的记忆，与AVL的对比
- **B树 / B+树**：多路查找树（数据库索引的基石）

### 一句话本质
BST解决了有序插入的查找效率，旋转是为了防止退化成链表。

### 对应Java底层影子
`HashMap`（红黑树）、`TreeMap`（红黑树）、`ConcurrentSkipListMap`

---

## Phase 8 — 树形结构（三）：堆与优先队列

### 核心通识知识点
- **堆（Heap）**：大根堆 / 小根堆的定义（逻辑上是完全二叉树）
- **堆化（建堆）**：从最后一个非叶子节点向下调整
- **优先队列（Priority Queue）**：出队总是优先级最高
- **堆排序**：利用堆进行选择排序

### 一句话本质
堆是"用数组表示的树"，专门用来快速找最大/最小值。

### 对应Java底层影子
`PriorityQueue`（二叉小根堆）

---

## Phase 9 — 散列表（哈希表）

### 核心通识知识点
- 哈希函数与哈希冲突
- **解决冲突的两种王道方法**：链地址法（拉链） vs 开放地址法（线性/平方探测）
- **装填因子（Load Factor）**与**动态扩容（Rehash）**
- 哈希表的查找/插入/删除的平均时间复杂度

### 一句话本质
哈希表是"键值对"映射，通过哈希函数将无限空间映射到有限空间。

### 对应Java底层影子
`HashMap`（链地址+红黑树）、`HashSet`、`LinkedHashMap`

---

## Phase 10 — 图（Graph）

### 核心通识知识点
- 图的定义：有向图 / 无向图，顶点 / 边
- 图的两种存储：**邻接矩阵**（稠密图）vs **邻接表**（稀疏图）
- **深度优先搜索（DFS）** 与**广度优先搜索（BFS）**
- **拓扑排序**（有向无环图DAG的依赖顺序）
- **最短路径**：Dijkstra（无负权）、Floyd（多源）
- **最小生成树**：Prim（加点）和 Kruskal（加边）概念

### 一句话本质
图是"多对多"的网状结构，树是图的特例（无环连通图）。

### 对应Java底层影子
无直接JCF对应，但`Spring`依赖注入的循环依赖检测用到了拓扑排序思想

---

## Phase 11 — 算法策略（思想篇）

### 核心通识知识点
- **分治**：大问题拆成相同子问题（归并/快排）
- **贪心**：每一步取局部最优（区间调度、哈夫曼树）
- **动态规划（DP）**：状态定义 + 状态转移方程（背包、LCS）
- **回溯**：试错 + 剪枝（全排列、N皇后）
- **查找算法**：顺序、二分（折半）、插值查找

### 一句话本质
算法思想是解决问题的"心法"，不依赖具体数据结构。

### 对应Java底层影子
`Collections.binarySearch()`（二分查找实现）

---

# 🧭 学习导航策略

1. **先纵向扫盲**：从上到下浏览一遍，如果发现某个名词（比如"线索二叉树"、"Floyd"）完全没听过，**很正常**，这张表就是要确保你**知道它的存在**。
2. **横向渐进深挖**：按 **Phase 0 -> Phase 1 -> Phase 2 ...** 的节奏推进。每个阶段，先讲**"逻辑定义"（是什么）**，再讲**"物理存储/实现细节"（为什么）**，最后才带出**"Java代码手写"（怎么落地）**。
