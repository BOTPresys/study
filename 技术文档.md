# study — Java 基础学习项目技术文档

## 项目概述

Java 基础语法练习项目，涵盖面向对象编程（封装、继承、多态）、集合框架、异常处理、数组操作等核心知识点。

- **语言**: Java 23
- **构建工具**: Maven
- **包结构**: `org.example`（无子包）
- **入口类**: `Main.java`

---

## 一、类关系图

```
BankAccount（父类）
    ↑
SavingAccount（子类）

InsufficientBalance（自定义异常，继承 RuntimeException）

工具类/演示类：
    Student      — HashSet 去重演示
    MapTest      — HashMap 词频统计
    DataDemo     — 数组操作算法
```

---

## 二、核心类详解

### 1. `BankAccount.java` — 银行账户（基类）

**设计目的**：演示封装、构造器重载、异常抛出。

| 属性 | 类型 | 可见性 | 说明 |
|------|------|--------|------|
| `accountId` | `String` | `private` | 账号 ID |
| `balance` | `double` | `protected` | 余额（允许子类直接访问） |

| 方法 | 说明 | 异常 |
|------|------|------|
| `deposit(double)` | 存款，金额需 > 0 | — |
| `withdraw(double)` | 取款 | 金额 ≤ 0 → `IllegalArgumentException`；余额不足 → `InsufficientBalance` |
| `setBalance(double)` | 余额不可为负，否则强置为 0 | — |
| `showInfo()` | 打印账号和余额 | — |

> `balance` 使用 `protected` 而非 `private`，方便子类直接操作。

---

### 2. `SavingAccount.java` — 储蓄账户（子类）

**继承关系**：`SavingAccount extends BankAccount`

**新增特性**：
- `interestRate`（利率，`double`）
- `addInterest()`：按利率自动增值

**重写 `withdraw()`**：
- 与父类行为不同：此版本使用 `System.out.println` 输出错误信息，未抛异常
- 设计说明：与父类对比展示两种错误处理策略（异常 vs 打印）

---

### 3. `InsufficientBalance.java` — 自定义异常

```
InsufficientBalance extends RuntimeException
```

- 无参构造 + 带消息构造
- 在 `BankAccount.withdraw()` 中余额不足时抛出

---

## 三、集合与数据结构演示类

### 4. `Student.java` — HashSet 去重

**学习目标**：理解 `equals()` 和 `hashCode()` 对 HashSet 去重的影响。

- 属性：`name`（String）、`age`（int）
- 重写了 `equals()` 和 `hashCode()`（基于 `Objects.hash()`）
- 两个属性相同的 `Student` 对象在 HashSet 中会被视为同一个元素

---

### 5. `MapTest.java` — 词频统计

**功能**：接收字符串数组，统计每个单词出现次数。

| 方法 | 说明 |
|------|------|
| `CountFrequency()` | 遍历数组 → HashMap 计数 → 打印结果 |

算法逻辑：`map.get(word) == null` → 首次出现，put 1；否则 value+1。

---

### 6. `DataDemo.java` — 数组操作算法

| 方法 | 说明 |
|------|------|
| `removeElement(int[] nums, int val)` | 原地移除指定值，返回新长度（LeetCode 27 题） |

双指针（快慢指针）实现：
- `p` 指针标记有效元素写入位置
- 遍历数组，非目标值前移，目标值后置

---

## 四、入口类：`Main.java`

当前调用 `DataDemo.removeElement()` 演示。

上方注释留有**多线程同步**的学习笔记（`synchronized` 关键字），为后续扩展预留。

---

## 五、构建与运行

```bash
mvn compile                              # 编译
mvn exec:java -Dexec.mainClass="org.example.Main"  # 运行
```

## 六、依赖

| 依赖 | 版本 | 作用域 | 用途 |
|------|------|--------|------|
| TestNG | `RELEASE` | compile | 测试框架（预留，尚无测试） |

> 当前 `src/test/` 目录不存在，TestNG 暂未使用。
